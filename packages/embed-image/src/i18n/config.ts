/**
 * i18n 语言配置(@lokvis/embed-image)。
 *
 * 语言集合与默认语言统一来自 @lokvis/i18n 核心,此处仅 re-export;
 * 包内独立维护字典与 Provider(key 命名空间隔离)。
 */
export { languages, defaultLang, langList, type Language } from '@lokvis/i18n';
