---
'@lokvis/schema': minor
'@lokvis/capability': minor
'@lokvis/engine-image': patch
---

Open 侧 W14.5 交付:补齐 capability preset + schema 兼容性校验修复 + Phase 2 fan-out

cloud 侧 W14.5 已交付 10 个 seed workflow,部分因 open 侧 capability preset
缺失或 schema 校验限制无法完整实现。本次补齐 open 侧待办,解除 seed #4/#5/
#6/#7/#8/#9/#10 的兼容性校验阻塞,并提前实现 Phase 2 fan-out 节点 schema 支持。

- `@lokvis/schema` (minor):
  - `validateCapabilityCompatibility` 全节点覆盖 unknown capability。原 5a
    仅检查入度 0 节点,transform 节点的未注册 capability 在 5b 边检查中被
    静默跳过,现统一遍历所有带 capability 的节点显式报错。
  - Phase 2 fan-out 节点支持:`NodeType` 新增 `'fan-out'`,`workflowNodeSchema`
    新增对应枚举与 refine(fan-out 禁止带 capability);`validateWorkflow`
    新增 4b 阶段校验 fan-out 节点必须 ≥2 条出边。支持一个输入分流到多个
    并行子链(解除 seed #3/#6/#2 多平台并行输出限制)。
- `@lokvis/capability` (minor):
  - 新增 AUDIO 域 preset(audio.trim/normalize/denoise/transcode/merge)
    并聚合到 BUILTIN_CAPABILITIES(解除 seed #8 阻塞)
  - 新增 `pdf.add-page-numbers` capability(seed #4/#5)
  - 新增 `video.resize` / `video.crop` / `video.normalize-audio`(seed #6/#7)
  - `image.convert` format 枚举新增 `ico`(seed #9)
  - `image.watermark` 图片水印参数 `image` → `imagePath`(seed #10)
  - 新增 AI 域 preset(ai.generate-workflow/optimize-workflow/caption/ocr/
    background-remove),`ai.background-remove` 复用 AI 域不创建独立
    image.remove-background(§2.6 评估结论)
- `@lokvis/engine-image` (patch): `WatermarkParams.image` → `imagePath`,
  watermark 操作解构与 SSRF 守卫注释同步,行为不变。
