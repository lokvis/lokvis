# W16.2 5 Demo 页跑通验收报告（M1.2 Open Alpha）

> **报告日期**：2026-07-05
> **对应里程碑**：M1.2 Open Alpha（目标 2026.12.31）
> **对应任务**：`docs/PROJECT_PLAN.md` 16.2「5 demo 跑通」
> **执行环境**：CI sandbox（Ubuntu，Node 22 + pnpm 9.12.0，CI=true / no TTY）。本沙箱**非交互环境**——无浏览器、无 `createImageBitmap` / OPFS / IndexedDB 真实环境，无法执行真人交互验收（上传图片 / 点击 Run / 查看输出图）。本报告以「静态代码审查 + typecheck + build」三件套作为可重复执行的验收依据，运行时行为验证留待 W16.6 部署后由真人执行。

---

## 1. 验收范围

5 个核心 demo（W14.2-W14.6 落地）—— playground 的「开发者试用 @lokvis/sdk」主入口：

| # | Demo | 文件 | Astro 页面 | nav 位置 |
|---|------|------|-----------|----------|
| 1 | SdkDemo | `apps/playground/src/components/demos/SdkDemo.tsx` | `apps/playground/src/pages/sdk.astro` | Demos 组第 2 项 |
| 2 | RuntimeDemo | `apps/playground/src/components/demos/RuntimeDemo.tsx` | `apps/playground/src/pages/runtime.astro` | Demos 组第 3 项 |
| 3 | PluginDemo | `apps/playground/src/components/demos/PluginDemo.tsx` | `apps/playground/src/pages/plugin.astro` | Demos 组第 4 项 |
| 4 | WorkflowDemo | `apps/playground/src/components/demos/WorkflowDemo.tsx` | `apps/playground/src/pages/workflow.astro` | Demos 组第 5 项 |
| 5 | McpManifestDemo | `apps/playground/src/components/demos/McpManifestDemo.tsx` | `apps/playground/src/pages/mcp.astro` | Demos 组第 6 项 |

> 注：Demos 组第 1 项是 Code Editor（`index.astro`），不是 5 个核心 demo 之一。

---

## 2. 验收方法

由于本沙箱无浏览器，采用**「静态代码审查 + typecheck + build」三件套**作为可重复执行的验收依据：

| 维度 | 工具 | 命令 | 期望 |
|------|------|------|------|
| 类型检查 | turbo + tsc + astro check | `pnpm typecheck` | 36/36 包通过，0 errors |
| 构建 | turbo + astro build | `pnpm build` | 20/20 任务通过；playground 18 个 HTML 输出 |
| 静态代码审查 | 人工 Read | 5 个 demo .tsx + 5 个 .astro + PlaygroundLayout.astro | 5 步流程完整 / 关键 API 调用正确 / ErrorBoundary 包裹 / nav 链接存在 |
| 单元测试 | vitest | `pnpm test` | 43 files / 787 tests 通过（含 sentry 26 测试） |

> 沙箱无浏览器，**无法**验证：`createImageBitmap` 解码、Canvas `drawImage` 渲染、OPFS 写入、`URL.createObjectURL` 输出图显示、React 组件实际渲染效果。这些留待 W16.6 部署后由真人交互验证。

---

## 3. 各 Demo 验收明细

### 3.1 SdkDemo（SDK Basics）

**文件**：`apps/playground/src/components/demos/SdkDemo.tsx`（314 行）

**5 步流程**（注释 §1-9 明确「5 步入门」）：

| 步骤 | API 调用 | 代码位置 | 状态 |
|------|----------|----------|------|
| 1. createLokvis | `await createLokvis({ plugins: [imageToolsPlugin()] })` | L73 | ✅ |
| 2. capabilities | `await rt.capabilities()` 并按 namespace 分组 | L75 + L50-57 `groupByCategory` | ✅ |
| 3. importAsset | `await runtime.importAsset({ kind: 'file', file })` | L85 | ✅ |
| 4. run(workflow, [assetId]) | `await runtime.run(workflow, [inputId])`，单节点 `image.resize` width=400 | L117 + L100-114 workflow 定义 | ✅ |
| 5. exportAsset | `await runtime.exportAsset(result.outputs[0])` → 触发浏览器下载 | L130 | ✅ |

**关键 API 调用**：
- `createLokvis({ plugins: [imageToolsPlugin()] })` ✅
- `runtime.capabilities()` ✅
- `runtime.importAsset({ kind: 'file', file })` ✅
- `runtime.run(workflow, [assetId])` ✅
- `runtime.exportAsset(result.outputs[0])` ✅

**页面注册**：`apps/playground/src/pages/sdk.astro` ✅
```astro
<PlaygroundLayout title="Lokvis Playground · SDK Basics" active="sdk">
  <ErrorBoundary client:only="react">
    <SdkDemo />
  </ErrorBoundary>
</PlaygroundLayout>
```

**nav 链接**：`PlaygroundLayout.astro` `NAV_GROUPS` Demos 组第 2 项 ✅
```ts
{ id: 'sdk', label: 'SDK Basics', href: `${base}sdk`, desc: 'createLokvis() · capabilities · run' },
```

**额外特性**：示例代码面板（`<details>` 折叠）+ 一键 Copy 按钮 + 错误显示 + 状态徽标（ready/running/completed/failed）

**结论**：SdkDemo ✅

### 3.2 RuntimeDemo（Runtime Basics）

**文件**：`apps/playground/src/components/demos/RuntimeDemo.tsx`（304 行）

**5 步流程**：

| 步骤 | API 调用 | 代码位置 | 状态 |
|------|----------|----------|------|
| 1. createLokvis | `await createLokvis({ plugins: [imageToolsPlugin(), devToolsPlugin()] })` | L48 | ✅ |
| 2. capabilities | `await rt.capabilities()` | L50 | ✅ |
| 3. importAsset | `await runtime.importAsset({ kind: 'file', file })`（支持多文件批量上传） | L76 | ✅ |
| 4. listAssets | `await rt.listAssets()` + `runtime.removeAsset(id)` | L52 / L88 | ✅ |
| 5. run(resize executor) | `await runtime.run(workflow, [firstAsset.id])`，单节点 `image.resize` width=400 + `runtime.exportAsset(res.outputs[0])` 显示预览 | L124 / L130 | ✅ |

**关键 API 调用**：
- `createLokvis({ plugins: [imageToolsPlugin(), devToolsPlugin()] })` ✅（同时预加载 image + dev 插件）
- `runtime.capabilities()` ✅
- `runtime.importAsset({ kind: 'file', file })` ✅
- `runtime.listAssets()` ✅
- `runtime.eventBus.onAny((e) => addLog(...))` ✅（事件订阅 + cleanup）
- `runtime.run(workflow, [firstAsset.id])` ✅
- `runtime.exportAsset(res.outputs[0])` + `URL.createObjectURL(blob)` 显示预览图 ✅

**页面注册**：`apps/playground/src/pages/runtime.astro` ✅（结构同 sdk.astro）

**nav 链接**：`PlaygroundLayout.astro` Demos 组第 3 项 ✅
```ts
{ id: 'runtime', label: 'Runtime Basics', href: `${base}runtime`, desc: 'Import asset + list capabilities' },
```

**额外特性**：三栏布局（Capabilities / Assets / Event Log），事件日志支持 Clear，输出预览图 32×32 缩略

**结论**：RuntimeDemo ✅

### 3.3 PluginDemo（Plugin Loader）

**文件**：`apps/playground/src/components/demos/PluginDemo.tsx`（243 行）

**5 步流程**（动态加载）：

| 步骤 | API 调用 | 代码位置 | 状态 |
|------|----------|----------|------|
| 1. createLokvis（仅 image） | `await createLokvis({ plugins: [imageToolsPlugin()] })` | L70 | ✅ |
| 2. capabilities（初始） | `await rt.capabilities()`，记录 `initialCapNames` | L72-74 | ✅ |
| 3. loadPlugin(devToolsPlugin) | `await loadPlugin(runtime, info.factory())` 动态加载 dev 插件 | L101 | ✅ |
| 4. capabilities（重读） | `await runtime.capabilities()`，对比 before/after 找出新增能力 | L102-105 | ✅ |
| 5. plugin:loaded 事件订阅 | `rt.eventBus.on('plugin:loaded', (e) => addLog(...))` | L79-81 | ✅ |

**关键 API 调用**：
- `createLokvis({ plugins: [imageToolsPlugin()] })` ✅
- `loadPlugin(runtime, devToolsPlugin())` ✅（动态加载，image 9 → image+dev 12，+3 capabilities）
- `runtime.eventBus.on('plugin:loaded', handler)` ✅
- `runtime.capabilities()` ✅

**页面注册**：`apps/playground/src/pages/plugin.astro` ✅

**nav 链接**：Demos 组第 4 项 ✅
```ts
{ id: 'plugin', label: 'Plugin Loader', href: `${base}plugin`, desc: 'loadPlugin() dynamic capability' },
```

**额外特性**：
- 左栏分「Loaded Plugins」+「Available Plugins」两块
- 右栏 Capabilities 按命名空间分组，新加载的能力高亮 `bg-emerald-900/20` + `New` 徽标
- 底部 Event Log 显示 `plugin:loaded` 事件时间线

**结论**：PluginDemo ✅

### 3.4 WorkflowDemo（5-Step Workflow）

**文件**：`apps/playground/src/components/demos/WorkflowDemo.tsx`（352 行）

**5 步链式 workflow**（resize → watermark → rotate → filter → convert）：

| 步骤 | capability | 关键参数 | 节点 id | 状态 |
|------|-----------|----------|---------|------|
| 1. resize | `image.resize` | `width: targetWidth, height: 0, fit: 'inside'`（默认 600） | n1 | ✅ |
| 2. watermark | `image.watermark` | `text: watermarkText, position: 'bottom-right', opacity: 0.5, fontSize: 24, color: '#ffffff'` | n2 | ✅ |
| 3. rotate | `image.rotate` | `angle: rotateAngle, background: '#ffffff'`（默认 90°） | n3 | ✅ |
| 4. filter | `image.filter` | `preset: filterPreset`（grayscale / invert / sepia / blur） | n4 | ✅ |
| 5. convert | `image.convert` | `format: outputFormat, quality: 90`（webp / jpeg / png / avif / gif） | n5 | ✅ |

**edges**（4 条，链式串联）：`n1→n2→n3→n4→n5` ✅

**per-node 耗时时间线**：
- 订阅 `runtime.eventBus.on('node:finished', (e) => setNodeTimings(prev => ({...prev, [e.nodeId]: e.duration})))` ✅
- 时间线 UI：5 个圆形节点 + 连接线（已完成节点 `bg-emerald-600`，未完成 `bg-zinc-800`）+ 每节点显示 `${nodeTimings[node.id]}ms` ✅

**关键 API 调用**：
- `createLokvis({ plugins: [imageToolsPlugin()] })` ✅
- `runtime.importAsset({ kind: 'file', file })` ✅
- `runtime.eventBus.on('node:finished', handler)` ✅
- `runtime.run(workflow, [inputId])` ✅（5 节点 + 4 edges）
- `runtime.exportAsset(res.outputs[0])` + 显示输入/输出对比 ✅

**页面注册**：`apps/playground/src/pages/workflow.astro` ✅

**nav 链接**：Demos 组第 5 项 ✅
```ts
{ id: 'workflow', label: '5-Step Workflow', href: `${base}workflow`, desc: 'resize → compress → watermark → rotate → convert' },
```

> 注：nav desc 文案是「resize → compress → watermark → rotate → convert」，实际代码是 `resize → watermark → rotate → filter → convert`（无 compress）。desc 文案与实际管线有 1 词差异（compress vs filter），不影响功能，留作未来文案修复。

**额外特性**：5 个可调参数面板（targetWidth / watermarkText / rotateAngle / filterPreset / outputFormat）+ 输入/输出图片对比 + Workflow Result JSON 折叠面板

**结论**：WorkflowDemo ✅

### 3.5 McpManifestDemo（MCP Manifest）

**文件**：`apps/playground/src/components/demos/McpManifestDemo.tsx`（184 行）

**核心流程**：

| 步骤 | API 调用 | 代码位置 | 状态 |
|------|----------|----------|------|
| 1. createLokvis（pluginSet 切换） | `image-only` → `[imageToolsPlugin()]`；`image+dev` → `[imageToolsPlugin(), devToolsPlugin()]` | L37-44 | ✅ |
| 2. toMcpManifest({ batchMode }) | `rt.toMcpManifest({ batchMode })` 输出 MCP manifest（tools + resources） | L43 / L51 | ✅ |
| 3. pluginSet 切换 | `useState<PluginSet>('image-only')`，切换时重建 runtime + 重生成 manifest | L30 / L47 useEffect dep | ✅ |
| 4. batchMode 切换 | `useState<boolean>(false)`，切换时 `setManifest(runtime.toMcpManifest({ batchMode }))` | L31 / L49-54 | ✅ |
| 5. 详情面板 | 选中 tool 后显示 `inputSchema` JSON + capabilities 列表 | L56 / L130-156 | ✅ |

**关键 API 调用**：
- `createLokvis({ plugins })` ✅
- `runtime.toMcpManifest({ batchMode })` ✅
- `manifest.tools` / `manifest.resources` 访问 ✅

**页面注册**：`apps/playground/src/pages/mcp.astro` ✅

**nav 链接**：Demos 组第 6 项 ✅
```ts
{ id: 'mcp', label: 'MCP Manifest', href: `${base}mcp`, desc: 'Expose capabilities as MCP tools' },
```

**额外特性**：
- 控制面板：pluginSet 下拉 + batchMode 复选框 + 顶部 tools/resources 数量统计
- 左栏 tools 列表（按钮选中高亮）+ 右栏详情面板（inputSchema JSON + capabilities 徽标）
- 完整 manifest JSON 折叠面板（供复制到 MCP client 配置）

**结论**：McpManifestDemo ✅

---

## 4. 整体构建验证

执行命令（2026-07-05 12:01-12:03 UTC）：

```bash
$ pnpm typecheck    # 36/36 包通过，0 errors
$ pnpm build        # 20/20 任务通过
$ pnpm test         # 43 files / 787 tests 通过，0 failed
```

### 4.1 typecheck

```
@lokvis/playground:typecheck: Result (56 files):
@lokvis/playground:typecheck: - 0 errors
@lokvis/playground:typecheck: - 0 warnings
@lokvis/playground:typecheck: - 0 hints

 Tasks:    36 successful, 36 total
  Time:    35.222s
```

### 4.2 build（playground 18 个 HTML）

```
$ find apps/playground/dist -name "*.html" | sort
apps/playground/dist/components/index.html
apps/playground/dist/history/index.html
apps/playground/dist/image/index.html
apps/playground/dist/index.html               ← Code Editor（Demos 第 1 项）
apps/playground/dist/mcp/index.html           ← McpManifestDemo（Demos 第 6 项）
apps/playground/dist/offline.html             ← SW fallback（非 demo）
apps/playground/dist/plugin/index.html        ← PluginDemo（Demos 第 4 项）
apps/playground/dist/runtime/index.html       ← RuntimeDemo（Demos 第 3 项）
apps/playground/dist/sdk/index.html           ← SdkDemo（Demos 第 2 项）
apps/playground/dist/workflow/index.html      ← WorkflowDemo（Demos 第 5 项）
apps/playground/dist/tools/batch/index.html
apps/playground/dist/tools/compress/index.html
apps/playground/dist/tools/convert/index.html
apps/playground/dist/tools/crop/index.html
apps/playground/dist/tools/download/index.html
apps/playground/dist/tools/resize/index.html
apps/playground/dist/tools/watermark-batch/index.html
apps/playground/dist/tools/watermark/index.html

 Tasks:    20 successful, 20 total
```

**HTML 页面分布**（17 个预渲染页 + 1 个 offline.html SW fallback）：
- **5 个核心 demo 页**：sdk / runtime / plugin / workflow / mcp ✅
- **1 个 Code Editor**：index ✅
- **3 个历史 demo**：image / history / components ✅
- **8 个单工具页**：tools/{batch, compress, convert, crop, download, resize, watermark, watermark-batch} ✅
- **1 个 SW fallback**：offline.html（非预渲染页，由 SW 在离线时返回）

### 4.3 ErrorBoundary 包裹

5 个 demo .astro 页面**全部**用 `<ErrorBoundary client:only="react">` 包裹 demo 组件（验证 sdk.astro 为例）：

```astro
<PlaygroundLayout title="..." active="sdk">
  <ErrorBoundary client:only="react">
    <SdkDemo />
  </ErrorBoundary>
</PlaygroundLayout>
```

- `ErrorBoundary` 位于 `apps/playground/src/components/ErrorBoundary.tsx` ✅
- 5 个 demo .astro（sdk / runtime / plugin / workflow / mcp）均使用 ErrorBoundary ✅
- `client:only="react"` 确保 React 组件仅在客户端渲染（Astro 静态站实际无 SSR）

**单 demo 崩溃不影响其他 demo / playground shell**：ErrorBoundary 隔离 + Sentry `captureException` 上报 ✅

### 4.4 单元测试

```
 Test Files  43 passed (43)
      Tests  787 passed (787)
   Duration  22.62s
```

**与 demo 相关的测试**：
- `apps/playground/src/toolkit/__tests__/sentry.test.ts`：26 测试（W12 PR #14 Review 重写）—— 覆盖 shouldEnableSentry / initSentry / beforeBreadcrumb / beforeSend / captureException / captureMessage / setAnonymousUser 七类场景
- `apps/playground/src/components/toolkit/__tests__/download.test.ts`：下载工具测试

> 注：5 个 demo 是 React UI 组件，依赖浏览器 API（Canvas / OPFS / IndexedDB / URL.createObjectURL），单测层面不直接覆盖（与 W12.1 验收策略一致）。运行时行为通过 typecheck + build + 真人交互验证。

---

## 5. 已知限制

| # | 限制 | 影响 | 缓解 |
|---|------|------|------|
| L1 | 沙箱无浏览器，无法验证 `createImageBitmap` 解码 / Canvas 渲染 / OPFS 写入 | 5 个 demo 的运行时行为（上传图片 / Run / 看输出图）未在沙箱验证 | W16.6 部署后由真人交互验证 |
| L2 | 沙箱无 IndexedDB / OPFS，无法验证 AssetStore 持久化 | 跨会话 Asset 持久化未在沙箱验证 | W16.6 部署后验证 |
| L3 | 沙箱无 SW 注册环境，无法验证 PWA 安装 / 离线 | SW 预缓存 / offline.html / InstallPrompt 未在沙箱验证 | W16.6 部署后验证 |
| L4 | WorkflowDemo nav desc 文案「resize → compress → watermark → rotate → convert」与实际管线「resize → watermark → rotate → filter → convert」有 1 词差异（compress vs filter） | 仅文案，不影响功能 | 未来文案修复（不阻塞 Alpha） |

---

## 6. 结论

| 维度 | 状态 | 说明 |
|------|------|------|
| 5 demo 代码就绪 | ✅ | 5 个 .tsx 文件完整，5 步流程清晰，关键 API 调用正确 |
| 5 demo 页面注册 | ✅ | 5 个 .astro 文件用 ErrorBoundary 包裹 + PlaygroundLayout active 高亮 |
| 5 demo nav 链接 | ✅ | PlaygroundLayout NAV_GROUPS Demos 组第 2-6 项 |
| typecheck | ✅ | 36/36 包通过，0 errors |
| build | ✅ | 20/20 任务通过，playground 18 HTML 输出（含 5 demo 页） |
| test | ✅ | 43 files / 787 tests 通过 |
| 运行时行为验证 | ⚠️ 待 | 沙箱无浏览器，留待 W16.6 部署后真人交互 |

**Go/No-Go**：**Go**（代码 + 构建 + 类型 + 测试全绿；运行时验证待 W16.6 部署后执行）
