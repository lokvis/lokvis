# M0.5.6.1: 迁移期防 SEO 重复（lokvis.app ↔ lokvis.com）
# 商业化内容已迁至 lokvis-cloud/apps/web（lokvis.com）
# 本站（lokvis.app/apps/web）将在 M0.5.7.1 整体删除
#
# 爬虫指令：禁止全站索引，允许 /workspace 供 PWA 离线访问
# 待 apps/web 删除后此文件随之移除

User-agent: *
Disallow: /

# 显式允许 Service Worker 与离线页（PWA 必需）
Allow: /sw.js
Allow: /offline.html
Allow: /manifest.webmanifest

# 不提交 sitemap（迁移期不索引）
