---
/**
 * G2:嵌入式音频转码工具页(cloud apps/web iframe 引用)。
 *
 * URL: /embed/en/tools/audio-transcode
 * 引用方:<iframe src="https://playground.lokvis.dev/embed/en/tools/audio-transcode" />
 */
import { langList } from '@/i18n/config';
import EmbedLayout from '@/layouts/EmbedLayout.astro';
import AudioTranscodeTool from '@/components/tools/audio/AudioTranscodeTool.tsx';

export function getStaticPaths() {
  return langList.map((lang) => ({ params: { lang } }));
}
---
<EmbedLayout slug="audio-transcode">
  <AudioTranscodeTool client:only="react" />
</EmbedLayout>
