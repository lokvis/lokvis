---
/**
 * G2:嵌入式视频压缩工具页(cloud apps/web iframe 引用)。
 *
 * URL: /embed/en/tools/video-compress
 * 引用方:<iframe src="https://playground.lokvis.dev/embed/en/tools/video-compress" />
 */
import { langList } from '@/i18n/config';
import EmbedLayout from '@/layouts/EmbedLayout.astro';
import VideoCompressTool from '@/components/tools/video/VideoCompressTool.tsx';

export function getStaticPaths() {
  return langList.map((lang) => ({ params: { lang } }));
}
---
<EmbedLayout slug="video-compress">
  <VideoCompressTool client:only="react" />
</EmbedLayout>
