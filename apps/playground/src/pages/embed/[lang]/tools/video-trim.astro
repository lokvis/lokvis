---
/**
 * G2:嵌入式视频裁剪工具页(cloud apps/web iframe 引用)。
 *
 * URL: /embed/en/tools/video-trim
 * 引用方:<iframe src="https://playground.lokvis.dev/embed/en/tools/video-trim" />
 */
import { langList } from '@/i18n/config';
import EmbedLayout from '@/layouts/EmbedLayout.astro';
import VideoTrimTool from '@/components/tools/video/VideoTrimTool.tsx';

export function getStaticPaths() {
  return langList.map((lang) => ({ params: { lang } }));
}
---
<EmbedLayout slug="video-trim">
  <VideoTrimTool client:only="react" />
</EmbedLayout>
