---
/**
 * G2:嵌入式批量水印工具页(cloud apps/web iframe 引用)。
 *
 * URL: /embed/en/tools/watermark-batch
 * 引用方:<iframe src="https://playground.lokvis.dev/embed/en/tools/watermark-batch" />
 */
import { langList } from '@/i18n/config';
import EmbedLayout from '@/layouts/EmbedLayout.astro';
import WatermarkBatchTool from '@/components/tools/WatermarkBatchTool.tsx';

export function getStaticPaths() {
  return langList.map((lang) => ({ params: { lang } }));
}
---
<EmbedLayout slug="watermark-batch">
  <WatermarkBatchTool client:only="react" />
</EmbedLayout>
