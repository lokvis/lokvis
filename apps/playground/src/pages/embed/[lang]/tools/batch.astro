---
/**
 * G2:嵌入式批量队列工具页(cloud apps/web iframe 引用)。
 *
 * URL: /embed/en/tools/batch
 * 引用方:<iframe src="https://playground.lokvis.dev/embed/en/tools/batch" />
 */
import { langList } from '@/i18n/config';
import EmbedLayout from '@/layouts/EmbedLayout.astro';
import BatchQueue from '@/components/tools/BatchQueue.tsx';

export function getStaticPaths() {
  return langList.map((lang) => ({ params: { lang } }));
}
---
<EmbedLayout slug="batch">
  <BatchQueue client:only="react" />
</EmbedLayout>
