---
/**
 * G2:嵌入式音频裁剪工具页(cloud apps/web iframe 引用)。
 *
 * URL: /embed/en/tools/audio-trim
 * 引用方:<iframe src="https://playground.lokvis.dev/embed/en/tools/audio-trim" />
 */
import { langList } from '@/i18n/config';
import EmbedLayout from '@/layouts/EmbedLayout.astro';
import AudioTrimTool from '@/components/tools/audio/AudioTrimTool.tsx';

export function getStaticPaths() {
  return langList.map((lang) => ({ params: { lang } }));
}
---
<EmbedLayout slug="audio-trim">
  <AudioTrimTool client:only="react" />
</EmbedLayout>
