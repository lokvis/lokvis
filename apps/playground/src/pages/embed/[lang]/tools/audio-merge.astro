---
/**
 * G2:嵌入式音频合并工具页(cloud apps/web iframe 引用)。
 *
 * URL: /embed/en/tools/audio-merge
 * 引用方:<iframe src="https://playground.lokvis.dev/embed/en/tools/audio-merge" />
 */
import { langList } from '@/i18n/config';
import EmbedLayout from '@/layouts/EmbedLayout.astro';
import AudioMergeTool from '@/components/tools/audio/AudioMergeTool.tsx';

export function getStaticPaths() {
  return langList.map((lang) => ({ params: { lang } }));
}
---
<EmbedLayout slug="audio-merge">
  <AudioMergeTool client:only="react" />
</EmbedLayout>
