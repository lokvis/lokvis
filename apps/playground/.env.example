# Lokvis Playground 环境变量示例
# 复制为 .env 并填入真实值;本地开发可不配置,所有变量均为可选

# Sentry 监控 DSN(W12.3)
# 未配置时 Sentry 模块退化为 no-op,不上报任何数据
# 获取方式:https://sentry.io/ → Settings → Projects → Client Keys
PUBLIC_SENTRY_DSN=

# Sentry release 标签(可选,默认 'playground@0.1.0')
# 建议在 CI 中注入 git SHA 或 package.json version
PUBLIC_SENTRY_RELEASE=playground@0.1.0
