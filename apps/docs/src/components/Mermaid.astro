---
/**
 * Mermaid 图表组件
 *
 * 通过 set:html 注入源码字符串,避免花括号被解析为 JSX 表达式。
 * 渲染脚本由 MermaidScript.astro 统一提供,每个页面只需引入一次。
 */
interface Props {
  source: string;
}
const { source } = Astro.props;
---

<div class="mermaid" set:html={source} />
